#include<stdio.h>
#include<string.h>
int main()
{
	char temp,s[50];
	scanf("%s",&s);
	int a=strlen(s);

	
		for(int i=0;i<a/2;i++)
		{
			temp=s[i];
			s[i]=s[a-(i+1)];
			s[a-(i+1)]=temp;
		}
	puts(s);
    
}
