#include<stdio.h>
#include<string.h>
int main()
{
	char c,s[50];
	int b=0;
	scanf("%s",s);
	int a=strlen(s);
    scanf(" %c",&c);//to make sure the lette that is given is stored in the variable
	
		for(int i=0;i<a;i++)
		{
			if(s[i]==c)
			b++;
		}
	printf("%d",b);
	}
