#include <stdio.h>
int main()
{
   int i,n,*p;
  scanf("%d",&n);
   int a[n];   
   for(i=0;i<n;i++)
      {
	  scanf("%d",a+i);
	  }
   p=&a[0];
   for(i=0;i<n;i++)
      {
	  printf("%d ",*p);
	  p++;
	  }
	   return 0;
}
