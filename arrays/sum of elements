#include <stdio.h>
int main()
{
    int n,sum;
       printf("size of the array here ");
       scanf("%d",&n);
       int arr[n];
       for(int i=0;i<n;i++)
        {
	     scanf("%d",&arr[i]);
	    }
	    for(int i=0;i<n;i++)
	    {
	    	sum+=arr[i];
		}
		printf("%d",sum);
	}
