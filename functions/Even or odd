#include<stdio.h>
int even_odd();
int even_odd(int n)
{ 
 if(n%2==0) 
 return 1;
 else 
 return 0;
}
int main()
{
   int num,ans;
   scanf("%d",&num);
   ans=even_odd(num);
   if (ans==1)
   printf("even");
   else if (ans==0)
   printf("odd");
   }
