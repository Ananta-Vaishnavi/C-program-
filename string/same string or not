#include <stdio.h>
int main()
{
    char str1[100],str2[100],t=0,n=0;
    gets(str1);
    gets(str2);
	
	for(int i=0;(str1[i]!=0&&str2[i]!=0);i++)
	{
    if(str1[i]==str2[i])  
	n++;
	t++;
	} 
	if(t==n)
   	printf("same");
 else
    printf("Not same");
}
